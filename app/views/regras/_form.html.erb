<%= form_for(@regra) do |f| %>
  <% if @regra.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@regra.errors.count, "error") %> prohibited this regra from being saved:</h2>

      <ul>
      <% @regra.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :criterio %><br>
    <%= f.text_field :criterio, size: "80%" %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
